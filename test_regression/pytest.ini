[pytest]
addopts = -v --tb=short --show-capture=no
markers = smoke: smoke set
    bindingsV2: tests that works only with meshsdk v2.* bindings
    bindingsV3: tests that works only with meshsdk v3.* bindings